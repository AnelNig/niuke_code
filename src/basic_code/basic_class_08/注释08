1.给定一个没有负数的数组arr代表一个容器，返回能装几格水？
2.给定一个数组arr，返回所有子数组的累加和中，最大的累加和。
3.生成窗口最大值数组。
4.两个字符串str1和str2，判断str2是不是由str1循环右移得到的。
5.返回一个字符串str向右循环右移k位后的结果
6.将字符串类型的数组strs中的字符串进行拼接，使所有字符串拼起来后形成的字符串具有最低字典序。
7.一个会议室不能两个项目同时宣讲。返回会议室一天最多的宣讲场次。
8.最大的leftMax与rightMax之差的绝对值